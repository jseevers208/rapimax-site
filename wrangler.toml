# Rapimax — Cloudflare Pages + D1
# Deploy: npx wrangler pages deploy dist --project-name rapimax
name = "rapimax"
compatibility_date = "2024-12-01"

[[d1_databases]]
binding = "DB"
database_name = "rapimax-db"
database_id = "" # Fill after: npx wrangler d1 create rapimax-db
