# Rapimax — Cloudflare Workers + Assets (unified platform)
name = "rapimax-site"
compatibility_date = "2024-12-01"

[assets]
directory = "./dist"

[[d1_databases]]
binding = "DB"
database_name = "rapimax-db"
database_id = "" # Fill after: npx wrangler d1 create rapimax-db
